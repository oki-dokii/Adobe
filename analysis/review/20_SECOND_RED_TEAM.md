# Phase 20 — Second Red Team (attack v1 as if a stranger wrote it)

Attacks on `19_REVISED_ARCHITECTURE.md` only.

## Coverage

W/G/D41/U-flow/AC12 lite now placed. AH8 still unexplained as **unspecified research**, not a silent drop. Topic G author wanted a skill folder — **residual disagreement**, justified by same compare pass. Acceptable.

## Missing skills

Still none that survive independence tests. Temptation to add `structured-data-integrity` — **reject again** (padding). Temptation to add `query-page-alignment` — **reject again**.

## Redundancy

H now owns same-time internal typed conflicts **and** off-site. That re-bloats H toward I.

**Fix for iteration-02:** same-time **on-site** typed conflicts move to **fact_store consumer in SK-I** renamed conceptually as “on-site consistency including time,” OR stay in H.

Second-pass choice: **on-site all internal typed conflicts → SK-I** (I-01 knowledge conflict is internal). **SK-H only off-site linked.** Cleaner than v1.

D vs X accordion — specified. CIT vs K types — specified. ENT vs H sameAs — specified.

## Root cause

D-before-K is correct and reduces parallelism; implementers may ignore — **test required**.

## Evidence

Enums help. Window N=2 still arbitrary — **tunable**, disclose.

## FP

W-03 on a genuine all-win product could FP. Gate: only pages self-named vs competitor with binary win glyphs; Low disclosure.

V multi-label unknown conservative may **under-suppress** SaaS price — better than over-suppress.

## FN

Linked-only H still misses aggregator (AH9). Keep defer.

Internal conflicts in I vs H (this attack) must be resolved or we get gaps.

## Site-type

W-03 SaaS-biased — V-F / comparison page existence gate (W-04).

## Runtime

D-before-CIT is extra serial. Cost small vs LLM. Skip-ladder still the real risk: dropping K3 would kill novelty — **protect list** in 13.

## LLM/code

OK.

## Novelty

OK if protect list enforced.

## Remediation

OK.

## Orchestration

H after K still OK. If H only off-site, can parallel with X.

## Contracts

Need `finding_type` closed list in LOCKED.

## Safety

OK.

## Implementability

Qualifier lexicon still not authored — **implementation task**, not architecture change.

## Material improvements from this pass

1. Move on-site typed conflicts entirely to SK-I; SK-H off-site only (undo v1 H-bloat).
2. W-03 gated to first-party comparison pages.
3. Protect-list in skip-ladder (never drop K3, dual-fetch facts, qualifier det, V).

No skill add/remove.
