# FINAL — If We Started Again Today

**Would we build the same marketplace folders?**  
**Yes — the same 10 names and the same entrypoint.**

**Would we build the same internals as v0 `analysis/`?**  
**No.**

## What would change (and did, in v3-locked)

1. Orchestration order: **D before** citation/answerability/handoff.
2. Page flags for extractability; findings with `parent_id` and closed `finding_type`.
3. U13–U18 as code, not a markdown table.
4. W as finding classes, not a vanished merge.
5. G as named CIT type, not an implicit regex.
6. D41 as a site finding plus sanitizer.
7. SK-I = on-site consistency (time + typed); SK-H = off-site linked only.
8. No search API; ENT degrades.
9. Skip-ladder with a **protect-list** so the product does not collapse into SEO.
10. Honest accepted FNs in `limitations[]`.

## What we would still refuse

11th schema skill; W folder; live ChatGPT; Lighthouse skill; llms.txt required; GEO 40%; AG15 as hard NLP findings; inventing AH8.

Starting today with the same research, we would skip v0’s “parallel all content skills” mistake and write LOCKED first.
