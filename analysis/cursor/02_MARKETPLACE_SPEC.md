# Marketplace spec (implementer)

Copy `../18_MARKETPLACE_MANIFEST.md` and `../17_MARKETPLACE_STRUCTURE.md`.

SKILL.md lean; taxonomies in references/; checks in scripts/. Frontmatter `name`+`description` required. `allowed-tools` is not a sandbox.

README must explain composition DAG and that crawl is shared infrastructure owned by the entrypoint.
