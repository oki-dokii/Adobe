This layer is presentation-only. It consumes canonical findings and uses the
existing engine's ordinal, zero-fabrication translation; it never detects a
new defect, estimates revenue, or calls an external service.
